import 'package:flutter/material.dart';

// === Base Colors === //
class AppColors {
  static const Color primary = Color(0xFF17CF73);
  static const Color info = Color(0xFF3B82F6);

  static const Color deepGreen = Color(0xFF0B3D2E);       // Rich forest green
  static const Color mediumGreen = Color(0xFF145A3E);     // Main card background
  static const Color lightGreen = Color(0xFF1E7A56);      // Accent / button green
  static const Color gold = Color(0xFFD4AF37);            // Primary gold
  static const Color softGold = Color(0xFFE8D48B);        // Lighter gold for text
  static const Color creamWhite = Color(0xFFF5F0E8);      // Warm cream
  static const Color warmWhite = Color(0xFFFAF7F2);      // Card surfaces
  static const Color textDark = Color(0xFF1A1A1A);        // Body text
  static const Color textMuted = Color(0xFF6B6B6B);       // Muted text
  static const Color cardShadow = Color(0x1A0B3D2E);
}
